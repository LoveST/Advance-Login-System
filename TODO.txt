    fix ViewController
- Update the loadView() function to enable pre-process the templates
- Combine all the required templates in a single variable to faster process them later on
- Process all the templates and flush them to the browser after the Core::class finished processing the data

Ex:
-----------

function loadView($templateName, $process = false){

    if($process){

    } else {

    }

}

function processView($view){

    // replace special tags

    // translate custom language tags

    // flush to browser

}